# Full Cycle Defafio NGINX

## Instuções para execução:

Executar o comando:

> docker compose up -d --build

## Limpando a Imagem 

Para apagar a imagem, containers e volume criados basta executar o comando: 

> docker compose down --rmi all --volumes